import { StyleSheet } from "react-native";

export const ModifyStyles = StyleSheet.create({
  textItem: {
    fontSize: 15,
  },
  row: {
    flexDirection: "row",
    alignItems: "center",
    borderColor: "lightblue",
    borderWidth: 3,
    borderRadius: 5,
    margin: 5,
    paddingEnd: 20,
    paddingStart: 20,
  },
});
